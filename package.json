{
  "name": "portfolio-root",
  "version": "1.0.0",
  "description": "Portfolio personnel de Thomas COLIN - vitrine de projets",
  "scripts": {
    "postinstall": "npm install --prefix BackEnd && npm install --prefix FrontEnd",
    "dev": "concurrently -n back,front -c blue,magenta \"npm run dev --prefix BackEnd\" \"npm run dev --prefix FrontEnd\"",
    "build": "npm run build --prefix FrontEnd",
    "test": "npm run test --prefix BackEnd"
  },
  "keywords": ["portfolio", "react", "express"],
  "author": "Thomas COLIN",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
