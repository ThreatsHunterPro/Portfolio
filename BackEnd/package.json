{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --watch data/projects.json --watch data/profile.json -r dotenv/config src/server.js",
    "start": "node -r dotenv/config src/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": "Thomas COLIN",
  "license": "ISC",
  "description": "API du portfolio : projets, profil et formulaire de contact",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "supertest": "^7.1.4",
    "vitest": "^4.1.8"
  }
}
